

namespace databases001
{
    public partial class Form1 : Form
    {

            Boolean Success = false; // 테이블 접속 여부
            String commandText = "";
            String rearPrimary = "";
            int connMode;
            string connStart = "Host=192.168.201.151;Username=postgres;," +
                "Password=12345678;Database=book_car";
        
        public Form1()
        {
            InitializeComponent();

            //group_insert_update.Visible = false;

            this.la_primary.Text = string.Format("");
            this.la_name.Text = string.Format("");
            this.la_humen.Text = string.Format("");
            this.la_date.Text = string.Format("");

            this.co_primary.Text = string.Format("");
            this.co_name.Text = string.Format("");
            this.co_humen.Text = string.Format("");
            this.co_date.Text = string.Format("");

        }

        void init_Column()
        {
            if(connMode==1)
            {
                this.txt_primary.Text = string.Format("ISBN");
                this.txt_Author.Text = string.Format("Authir");

                this.la_primary.Text = string.Format("ISBN");
                this.la_humen.Text = string.Format("Authir");

            }
            else if(connMode==2) 
            {
                this.txt_primary.Text = string.Format("Number");
                this.txt_Author.Text = string.Format("Company");

                this.la_primary.Text = string.Format("Number");
                this.la_humen.Text = string.Format("Company");
            }
            this.txt_name.Text = string.Format("Name");
            this.txt_date.Text = string.Format("Date");
            this.la_name.Text = string.Format("Name");
            this.la_date.Text = string.Format("Date");
        }

        void init_txt()
        {
            txt_primary.Text = string.Empty;
            txt_name.Text = string.Empty;
            txt_Author.Text = string.Empty;
            txt_date.Text = string.Empty;
        }

        void init_Column_Size()
        {
            for (int i=0; i<listView1.Columns.Count; i++) 
            {
                listView1.Columns[i].Width = -2;
            }
        }

        void Reading_date(NpgsqlCommand cmd)
        {
            listView1.Items.Clear();
            using (var reader = cmd.ExecuteReader()) 
            {
                ListViewItem item;

                while (reader.Read()) 
                {

                }
            }

        }

        private void label2_Click(object sender, EventArgs e)
        {

        }

        private void label1_Click(object sender, EventArgs e)
        {

        }

        private void button3_Click(object sender, EventArgs e)
        {

        }
    }
 }
